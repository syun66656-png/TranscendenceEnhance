# TradeRestrict settings
restrict-keyword: "거래 금지"

virtual-storage:
  plugin-name: "VirtualStorage"
  # "holder" checks inventory holder ownership by reflection.
  # "api" additionally attempts a configured static API method.
  mode: "holder"
  api-class: ""
  api-method: "isVirtualInventory"

debug: false
